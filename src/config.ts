/**
 * Canonical site URL for structured data (JSON-LD).
 * Single source of truth — update here when switching domains.
 * Should match `site` in astro.config.mjs once the custom domain is live.
 */
export const SITE_URL = 'https://nurbanum.de';
